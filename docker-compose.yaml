version: '3.8'
services:
    client:
        build: ./
        image: jbsdevden/mern-tms-client
        container_name: client
        ports:
            - '3000:3000'
        volumes:
            - ./:/app
            - /app/node_modules
        stdin_open: true
        tty: true
        environment:
            CHOKIDAR_USEPOLLING: 'true'
